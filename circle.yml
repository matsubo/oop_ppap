machine:
  # Version of ruby to use
  ruby:
    version:
      2.3.0
  environment:
    RAILS_ENV: test
    RACK_ENV: test


general:
  artifacts:
    - "~/simplecov" # relative to the user's home directory

test:
  override:
    - bundle exec ruby main_test.rb

